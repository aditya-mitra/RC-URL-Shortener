export enum zeroConfigs { // eslint-disable-line
  id = 'zero_config_provider',
  cleanuri = 'cleanuri',
  shrtcode = 'shrtcode',
  tly = 'tly',
  tinyuid = 'tinyuid',
  zws = 'zerowidthshortener',
}
